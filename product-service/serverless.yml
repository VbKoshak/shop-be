# plugins:
#   - serverless-webpack

# custom:
#   webpack:
#     webpackConfig: 'webpack.config.js' # Name of webpack configuration file
#     includeModules: false # Node modules configuration for packaging
#     packager: 'npm' # Packager that will be used to package your external modules
#     excludeFiles: handlers/tests/*.test.js # Provide a glob for files to ignore

package:
  individually: true

service: product-service
frameworkVersion: '2'

provider:
  name: aws
  runtime: nodejs12.x
  lambdaHashingVersion: 20201221
  stage: dev
  region: us-east-2

  environment: 
    PG_HOST: postgress-task4.ciqc4bkqwwch.us-east-2.rds.amazonaws.com
    PG_PORT: 5432
    PG_DATABASE: task4
    PG_USERNAME: postgres
    PG_PASSWORD: superAdmin

functions:
  getProducts:
    handler: handlers/db/getProductList.main
    events:
      - http:
          path: products
          method: get
          cors: true
  getAvailableProducts:
    handler: handlers/db/getAvailableProductList.main
    events: 
      - http:
          path: products/available
          method: get
          cors: true
  getProductById:
    handler: handlers/db/getProductById.main
    events:
      - http:
          path: products/{productId}
          method: get
          cors: true
          request:
            parameters:
              paths:
                productId: true
  postProduct:
    handler: handlers/db/postProduct.main
    events:
      - http:
          path: products
          method: post
          cors: true
